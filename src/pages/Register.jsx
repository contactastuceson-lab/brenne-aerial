import { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import { ArrowRight, CheckCircle2, Eye, EyeOff, KeyRound, Loader2, Lock, Mail } from 'lucide-react';
import { base44 } from '@/api/base44Client';
import { Input } from '@/components/ui/input';
import AuthBrandPanel from '@/components/auth/AuthBrandPanel';

export default function Register() {
  const [step, setStep] = useState('form'); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [confirmPassword, setConfirmPassword] = useState(''); const [otpCode, setOtpCode] = useState(''); const [showPassword, setShowPassword] = useState(false); const [loading, setLoading] = useState(false); const [error, setError] = useState('');
  const register = async event => { event.preventDefault(); setError(''); if (password !== confirmPassword) return setError('Les mots de passe ne correspondent pas.'); if (password.length < 6) return setError('Le mot de passe doit contenir au moins 6 caractères.'); setLoading(true); try { await base44.auth.register({ email, password }); setStep('otp'); } catch (err) { setError(err?.message || 'Impossible de créer ce compte.'); } finally { setLoading(false); } };
  const verify = async event => { event.preventDefault(); setError(''); setLoading(true); try { const result = await base44.auth.verifyOtp({ email, otpCode }); base44.auth.setToken(result.access_token); const _ref = typeof localStorage !== 'undefined' ? localStorage.getItem('eza_ref_code') : ''; window.location.href = _ref ? '/parrainage' : '/'; } catch { setError('Code incorrect ou expiré.'); } finally { setLoading(false); } };
  useEffect(() => { const p = new URLSearchParams(window.location.search); const r = p.get('ref'); if (r) { localStorage.setItem('eza_ref_code', r); window.history.replaceState({}, '', window.location.pathname); } }, []);
  return <div className="flex min-h-screen bg-background"><AuthBrandPanel mode="register" /><main className="flex flex-1 items-center justify-center px-6 py-12"><div className="w-full max-w-md">{step === 'form' ? <><div className="mb-8 lg:hidden"><img src="https://media.base44.com/images/public/69c5c081406b9e20deaed582/80278201e_1782606023373-Photoroom.png" alt="eza" className="h-12 w-12 object-contain" /></div><h1 className="font-grotesk text-3xl font-black tracking-tight">Rejoindre eza</h1><p className="mt-2 font-inter text-sm text-muted-foreground">Créez votre espace et entrez dans la conversation.</p><div className="mt-8 grid grid-cols-3 gap-2"><button type="button" onClick={() => base44.auth.loginWithProvider('google', '/')} className="flex h-11 items-center justify-center gap-1 rounded-xl border border-border bg-card font-inter text-xs font-semibold hover:bg-secondary"><span className="text-base">G</span><span className="hidden sm:inline">Google</span></button><button type="button" onClick={() => base44.auth.loginWithProvider('facebook', '/')} className="flex h-11 items-center justify-center gap-1 rounded-xl border border-border bg-card font-inter text-xs font-semibold hover:bg-secondary"><span className="text-base text-blue-500">f</span><span className="hidden sm:inline">Facebook</span></button><button type="button" onClick={() => base44.auth.loginWithProvider('microsoft', '/')} className="flex h-11 items-center justify-center gap-1 rounded-xl border border-border bg-card font-inter text-xs font-semibold hover:bg-secondary"><span className="grid grid-cols-2 gap-px"><i className="h-1.5 w-1.5 bg-red-500" /><i className="h-1.5 w-1.5 bg-green-500" /><i className="h-1.5 w-1.5 bg-blue-500" /><i className="h-1.5 w-1.5 bg-yellow-400" /></span><span className="hidden sm:inline">Microsoft</span></button></div><div className="my-6 flex items-center gap-3"><span className="h-px flex-1 bg-border" /><span className="font-inter text-xs text-muted-foreground">ou avec votre email</span><span className="h-px flex-1 bg-border" /></div><form onSubmit={register} className="space-y-4"><Field icon={Mail} label="Adresse email"><Input type="email" value={email} onChange={e => setEmail(e.target.value)} className="h-11 pl-9" autoComplete="email" required /></Field><Field icon={Lock} label="Mot de passe"><Input type={showPassword ? 'text' : 'password'} value={password} onChange={e => setPassword(e.target.value)} className="h-11 pl-9 pr-10" autoComplete="new-password" required /><button type="button" onClick={() => setShowPassword(!showPassword)} className="absolute right-3 top-3 text-muted-foreground">{showPassword ? <EyeOff className="h-4 w-4" /> : <Eye className="h-4 w-4" />}</button></Field><Field icon={Lock} label="Confirmer le mot de passe"><Input type="password" value={confirmPassword} onChange={e => setConfirmPassword(e.target.value)} className="h-11 pl-9" autoComplete="new-password" required /></Field>{confirmPassword && password === confirmPassword && <p className="flex items-center gap-1 text-xs text-green-400"><CheckCircle2 className="h-3.5 w-3.5" /> Les mots de passe correspondent</p>}{error && <p className="rounded-xl border border-destructive/30 bg-destructive/10 px-3 py-2.5 font-inter text-xs text-destructive">{error}</p>}<button disabled={loading} className="flex h-11 w-full items-center justify-center gap-2 rounded-xl bg-primary font-inter text-sm font-bold text-primary-foreground disabled:opacity-60">{loading ? <Loader2 className="h-4 w-4 animate-spin" /> : <>Créer mon compte <ArrowRight className="h-4 w-4" /></>}</button></form><p className="mt-8 text-center font-inter text-sm text-muted-foreground">Déjà inscrit ? <Link to="/login" className="font-semibold text-primary hover:underline">Se connecter</Link></p></> : <><div className="mb-6 flex h-14 w-14 items-center justify-center rounded-2xl bg-primary/10"><KeyRound className="h-6 w-6 text-primary" /></div><h1 className="font-grotesk text-3xl font-black">Vérifiez votre email</h1><p className="mt-2 font-inter text-sm text-muted-foreground">Saisissez le code reçu à <strong className="text-foreground">{email}</strong>.</p><form onSubmit={verify} className="mt-8 space-y-4"><Input value={otpCode} onChange={e => setOtpCode(e.target.value.replace(/\D/g, ''))} maxLength={6} inputMode="numeric" className="h-14 text-center font-mono text-xl tracking-[0.55em]" placeholder="000000" autoFocus />{error && <p className="rounded-xl border border-destructive/30 bg-destructive/10 px-3 py-2.5 text-xs text-destructive">{error}</p>}<button disabled={loading || otpCode.length !== 6} className="flex h-11 w-full items-center justify-center gap-2 rounded-xl bg-primary font-inter text-sm font-bold text-primary-foreground disabled:opacity-60">{loading ? <Loader2 className="h-4 w-4 animate-spin" /> : <>Vérifier mon compte <ArrowRight className="h-4 w-4" /></>}</button></form><button onClick={() => base44.auth.resendOtp(email)} className="mt-5 text-sm font-semibold text-primary hover:underline">Renvoyer le code</button></>}</div></main></div>;
}
function Field({ icon: Icon, label, children }) { return <label className="block"><span className="mb-1.5 block font-inter text-xs font-semibold">{label}</span><span className="relative block"><Icon className="absolute left-3 top-3.5 h-4 w-4 text-muted-foreground" />{children}</span></label>; }